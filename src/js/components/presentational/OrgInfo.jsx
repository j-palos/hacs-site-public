import React from "react";
import PropTypes from "prop-types";

const OrgInfo = () => (
    <div>
        <p>Container for Info about what we do can go here</p>
    </div>
);

OrgInfo.propTypes = {};

export default OrgInfo;
