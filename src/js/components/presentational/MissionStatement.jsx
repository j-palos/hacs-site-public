import React from "react";
import PropTypes from "prop-types";

const MissionStatement = () => (
    <div>
        <p>
           container for info about the HACS mission statement
        </p>
    </div>
);

MissionStatement.propTypes = {};

export default MissionStatement;
